export class RaidTierConfiguration {
    // This file contains values that may need to be updated each time a new raid tier is released.
    // We should aim for this to be the only location in the code that requires frequent changes.
    // Expansion features (such as covenants) are an exception to this, since they require actual code changes.

    // Shadowlands
    public static readonly CURRENT_EXPANSION_ID: number = 499;

    // Sanctum of Domination
    public static readonly CURRENT_RAID_TIER_ID: number = 1193;
}
